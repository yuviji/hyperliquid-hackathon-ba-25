{
  "name": "loopops-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for LoopOps",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network hyperevm"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^17.2.3",
    "hardhat": "^2.22.0"
  }
}
